//type inference é quando não precisamos definir um tipo porque o typescript já entend 

const teste = "teste" // o typescript já entende que é uma string

window.addEventListener('click', (e) => {
    console.log(e.target);
})